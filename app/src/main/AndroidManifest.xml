<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rolplay">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/mainlogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/mainlogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApp">
        <activity android:name=".Servicios.MenuPersonajesActivity" android:screenOrientation="nosensor"/>
        <activity android:name=".Activities.InformacionActivity" android:screenOrientation="nosensor" />
        <activity
            android:name=".Activities.ContenedorInicioActivity"
            android:screenOrientation="nosensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.MainActivity"
            android:screenOrientation="nosensor" />
        <receiver android:name=".Otros.NotificationActionService"/>
        <service android:name=".Otros.OnClearFromRecentService"/>
    </application>

</manifest>